/**
 * Runs performances for different animals using polymorphism.
 */
public class AnimalShow {
    /**
     * Performs the animal's own act.
     *
     * @param animal the animal that should perform
     */
    public void perform(Animal animal) {
        animal.perform();
    }
}
